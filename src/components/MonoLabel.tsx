import { styled } from "@mui/material/styles";
import Typography from "@mui/material/Typography";

const MonoLabel = styled(Typography)({
  fontFamily: "'JetBrains Mono', monospace",
  fontSize: "0.75rem",
  color: "hsl(215, 20%, 65%)",
});

export default MonoLabel;
